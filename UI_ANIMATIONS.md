# 🎬 S7 Robotics — Полная документация анимаций

Этот документ описывает ВСЕ анимации используемые в приложении с точными таймингами, задержками и визуализацией.

---

## 📋 ОГЛАВЛЕНИЕ
1. [Глобальные keyframes](#-глобальные-keyframes)
2. [CSS переменные анимаций](#-css-переменные)
3. [Страница входа — анимации](#-страница-входа)
4. [Sidebar анимации](#-sidebar)
5. [Dashboard табы](#-dashboard-табы)
6. [Toast уведомления](#-toast-уведомления)
7. [Hover эффекты](#-hover-эффекты)
8. [Доступность](#-доступность)

---

## 🎭 ГЛОБАЛЬНЫЕ KEYFRAMES

### 1. slide-up (основная анимация появления)
```css
@keyframes slide-up {
  from { 
    opacity: 0; 
    transform: translateY(30px); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}
/* Класс: .animate-slide-up */
/* Длительность: 0.6s */
/* Easing: ease-out */
/* fill-mode: forwards */
```

**Визуализация:**
```
Кадр 0%:   ░░░░░░░░░░  opacity: 0, Y: +30px
           ↓
           ↓  (0.6 секунды)
           ↓
Кадр 100%: ██████████  opacity: 1, Y: 0
```

---

### 2. fade-in (мягкое появление)
```css
@keyframes fade-in {
  from { 
    opacity: 0; 
    transform: translateY(20px); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}
/* Класс: .animate-fade-in */
/* Длительность: 0.6s */
/* Easing: ease-out */
```

---

### 3. fade-in-up (появление снизу вверх)
```css
@keyframes fade-in-up {
  from { 
    opacity: 0; 
    transform: translateY(30px); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}
/* Класс: .animate-fade-in-up */
/* Длительность: 0.5s */
/* Easing: ease-out */
/* fill-mode: forwards */
```

---

### 4. slide-in-left (появление слева)
```css
@keyframes slide-in-left {
  from { 
    opacity: 0; 
    transform: translateX(-30px); 
  }
  to { 
    opacity: 1; 
    transform: translateX(0); 
  }
}
/* Класс: .animate-slide-in-left */
/* Длительность: 0.5s */
/* Easing: ease-out */
```

**Для**: мобильный sidebar

---

### 5. slide-in-bottom (сильный подъём снизу)
```css
@keyframes slide-in-bottom {
  from { 
    opacity: 0; 
    transform: translateY(50px);  /* Больше чем slide-up! */
  }
  to { 
    opacity: 1; 
    transform: translateY(0); 
  }
}
/* Класс: .animate-slide-in-bottom */
/* Длительность: 0.8s — медленнее */
/* Easing: ease-out */
```

---

### 6. fade-in-scale (появление с масштабом)
```css
@keyframes fade-in-scale {
  from { 
    opacity: 0; 
    transform: scale(0.96); 
  }
  to { 
    opacity: 1; 
    transform: scale(1); 
  }
}
/* Класс: .animate-fade-in-scale */
/* Длительность: 0.5s */
```

**Для**: модальные окна, диалоги

---

## ⏱️ CSS ПЕРЕМЕННЫЕ

```css
:root {
  /* Кривая ускорения — плавное начало и конец */
  --easing: cubic-bezier(0.2, 0.8, 0.2, 1);
  
  /* Быстрые переходы (hover, focus) */
  --dur-fast: 200ms;
  
  /* Средние переходы (анимации элементов) */
  --dur-mid: 300ms;
}
```

---

## 🔐 СТРАНИЦА ВХОДА (/) — ПОЛНЫЙ РАЗБОР

### Последовательность появления элементов:

```
ВРЕМЯ (ms)     ЭЛЕМЕНТ                      АНИМАЦИЯ
───────────────────────────────────────────────────────────────
    0ms        Фон (body::before)           Мгновенно
    ↓
  200ms        Логотип S7                   animate-slide-up
    ↓                                       (opacity 0→1, Y +30→0)
    ↓
  200ms        Карточка входа               animate-slide-up
    ↓                                       (одновременно с логотипом)
    ↓
  600ms        Поле "Полное имя"            animate-slide-up, delay: 600ms
    ↓                                       (только при регистрации!)
    ↓    
  650ms        Поле "Возраст"               animate-slide-up, delay: 650ms
    ↓
  700ms        Поле "Уч. заведение"         animate-slide-up, delay: 700ms
    ↓
  700ms        Поле "Почта"                 animate-slide-up, delay: 700ms
    ↓                                       (для входа тоже 700ms)
    ↓
  750ms        Выбор роли (3 карточки)      animate-slide-up, delay: 750ms
    ↓
  800ms        Поле "Пароль"                animate-slide-up, delay: 800ms
    ↓
 1000ms        Кнопка "Войти"               (часть формы)
    ↓
 1000ms        "Забыли пароль?"             animate-slide-up, delay: 1000ms
    ↓
 1000ms        "Нет аккаунта?..."           animate-slide-up, delay: 1000ms
    ↓
 1400ms        SocialPanel                  (появляется позже)
    ↓
 1400ms        "Пользовательские согл."     animate-slide-up, delay: 1400ms
    ↓
 1600ms        Блок "Обновление 1.0"        animate-slide-up, delay: 1600ms
    ↓
 1800ms        Footer (адреса, версия)      animate-slide-up, delay: 1800ms
```

### Визуальная схема:

```
0ms─────────────────────────────────────────────────────────────2000ms

ЛОГОТИП:        ████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░
                ↑ start: 200ms, duration: 600ms

КАРТОЧКА:       ████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░
                ↑ start: 200ms

ПОЛЯ ВВОДА:     ░░░░░░░░░░████████░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░
                          ↑ start: 600-800ms (каскад)

КН. ВОЙТИ:      ░░░░░░░░░░░░░░░░░░░███░░░░░░░░░░░░░░░░░░░░░░░░░░
                                   ↑ ~1000ms

СОЦСЕТИ:        ░░░░░░░░░░░░░░░░░░░░░░░░░░░░████░░░░░░░░░░░░░░░░
                                            ↑ 1400ms

FOOTER:         ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░████░░░░░░░░░░
                                                  ↑ 1800ms
```

---

### Код задержек (из app/page.tsx):

```tsx
{/* Логотип */}
<div className="animate-slide-up" style={{ animationDelay: "200ms" }}>
  <Image src="/logo-s7.png" ... />
</div>

{/* Карточка формы */}
<div className="animate-slide-up" style={{ animationDelay: "200ms" }}>
  ...
</div>

{/* Поле "Полное имя" (регистрация) */}
<div className="animate-slide-up" style={{ animationDelay: "600ms" }}>
  <Input placeholder="Полное имя" />
</div>

{/* Поле "Возраст" */}
<div className="animate-slide-up" style={{ animationDelay: "650ms" }}>
  <Input type="number" placeholder="Возраст" />
</div>

{/* Поле "Учебное заведение" */}
<div className="animate-slide-up" style={{ animationDelay: "700ms" }}>
  <Input placeholder="Учебное заведение" />
</div>

{/* Выбор роли */}
<div className="animate-slide-up" style={{ animationDelay: "750ms" }}>
  <div className="grid grid-cols-3">
    <button>Ученик</button>
    <button>Ментор</button>
    <button>Родитель</button>
  </div>
</div>

{/* Поле "Почта" */}
<div className="animate-slide-up" style={{ animationDelay: "700ms" }}>
  <Input type="email" placeholder="Почта" />
</div>

{/* Поле "Пароль" */}
<div className="animate-slide-up" style={{ animationDelay: "800ms" }}>
  <Input type="password" placeholder="Пароль" />
</div>

{/* Забыли пароль? */}
<div className="animate-slide-up" style={{ animationDelay: "1000ms" }}>
  <button>Забыли пароль?</button>
</div>

{/* Переключение вход/регистрация */}
<div className="animate-slide-up" style={{ animationDelay: "1000ms" }}>
  <button>Нет аккаунта? Зарегистрируйтесь</button>
</div>

{/* Соглашения */}
<div className="animate-slide-up" style={{ animationDelay: "1400ms" }}>
  Пользовательские соглашения
</div>

{/* Блок обновления */}
<div className="animate-slide-up" style={{ animationDelay: "1600ms" }}>
  Обновление 1.0
</div>

{/* Footer */}
<div className="animate-slide-up" style={{ animationDelay: "1800ms" }}>
  ИП АМАНТАЙ... Version 0.1
</div>
```

---

### Переход между режимами (Вход ↔ Регистрация):

```css
/* Анимация появления/скрытия дополнительных полей */
.transition-all {
  transition-property: all;
}
.duration-500 {
  transition-duration: 500ms;
}
.ease-in-out {
  transition-timing-function: ease-in-out;
}

/* Состояние: ПОКАЗАТЬ */
max-height: 500px;
opacity: 1;
transform: translateY(0);

/* Состояние: СКРЫТЬ */
max-height: 0;
opacity: 0;
transform: translateY(-16px);  /* -translate-y-4 = -16px */
```

**Визуально:**
```
ВХОД → РЕГИСТРАЦИЯ:
┌────────────────────┐
│ Почта              │  ← существующие поля остаются
│ Пароль             │
└────────────────────┘
          ↓
          ↓ 500ms ease-in-out
          ↓
┌────────────────────┐
│ Полное имя         │  ← новые поля выезжают сверху
│ Возраст            │     max-height: 0 → 500px
│ Учебное заведение  │     opacity: 0 → 1
│ Выбор роли         │     translateY: -16px → 0
├────────────────────┤
│ Почта              │
│ Пароль             │
└────────────────────┘
```

---

### Выбор роли — анимация нажатия:

```css
/* Обычное состояние */
transition-all duration-300

/* При клике */
active:scale-95  /* Уменьшение на 5% */

/* Активная роль */
bg-[var(--color-primary)]        /* #00a3ff */
border-[var(--color-primary)]
text-white
shadow-[0_4px_12px_rgba(0,163,255,0.3)]  /* Синее свечение */

/* Неактивная роль */
bg-transparent
border-[#1f1f1f]
text-[#a7a7a7]
hover:border-[#2a2a2a]
hover:text-white
hover:bg-[var(--color-surface-2)]
```

---

## 📋 SIDEBAR

### Мобильное открытие:

```css
/* Overlay (затемнение) */
.animate-fade-in {
  animation: fade-in 0.6s ease-out;
}

/* Сам sidebar */
.animate-slide-in-left {
  animation: slide-in-left 0.5s ease-out;
}
/* from: opacity 0, translateX(-30px) */
/* to:   opacity 1, translateX(0) */
```

**Схема:**
```
ЗАКРЫТО                         ОТКРЫТО
┌─────────────────────────────┐  ┌───────────┬──────────────────┐
│                             │  │  SIDEBAR  │  OVERLAY 50%     │
│      КОНТЕНТ                │→ │  w-64     │  click=закрыть   │
│                             │  │           │                  │
└─────────────────────────────┘  └───────────┴──────────────────┘
                                      ↑
                                 slide-in-left
                                 0.5s ease-out
```

### Сворачивание/разворачивание:

```css
/* Transition для изменения ширины */
transition-all duration-[var(--dur-mid)] ease-[var(--easing)]
/* = transition: all 300ms cubic-bezier(0.2, 0.8, 0.2, 1) */

/* Ширина */
w-64 (256px) ↔ w-16 (64px)
```

### Пункты навигации — появление:

```tsx
{navItems.map((item, index) => (
  <div
    className="animate-slide-up"
    style={{ animationDelay: `${index * 50}ms` }}
  >
    ...
  </div>
))}
```

**Результат:**
```
Пункт 1: delay 0ms    ████░░░░░░░░░░░░░░
Пункт 2: delay 50ms   ░░████░░░░░░░░░░░░
Пункт 3: delay 100ms  ░░░░████░░░░░░░░░░
Пункт 4: delay 150ms  ░░░░░░████░░░░░░░░
Пункт 5: delay 200ms  ░░░░░░░░████░░░░░░
```

### Hover на пункте:

```css
/* Неактивный пункт */
text-[var(--color-text-3)]  /* #a7a7a7 */
transition-all duration-300 ease-[var(--easing)]

/* Hover */
hover:text-[var(--color-text-1)]  /* белый */
hover:bg-[var(--color-surface-2)]  /* #0f0f0f */

/* Иконка при hover */
group-hover:scale-105
transition-transform duration-300
```

### Tooltip (свёрнутый режим):

```css
/* Скрытое состояние */
opacity: 0;
pointer-events: none;
transition: opacity 200ms;

/* При hover на пункте */
group-hover:opacity-100;
```

---

## 🏠 DASHBOARD ТАБЫ

### Карточки с каскадной анимацией:

```tsx
{/* home-tab.tsx */}
{continueCourses.map((c, idx) => (
  <div
    className="animate-slide-up hover:scale-102"
    style={{ animationDelay: `${300 + idx * 100}ms` }}
  >
    ...
  </div>
))}
```

**Пример для 3 курсов:**
```
Курс 1: delay 300ms   ░░░░░░███████░░░░░░░░░░░░░░
Курс 2: delay 400ms   ░░░░░░░░░░███████░░░░░░░░░░
Курс 3: delay 500ms   ░░░░░░░░░░░░░░░░███████░░░░
```

### Заголовки секций:

```tsx
<h2 
  className="animate-slide-up"
  style={{ animationDelay: "200ms" }}
>
  Continue learning
</h2>

<h2 
  className="animate-slide-up"
  style={{ animationDelay: "800ms" }}
>
  News
</h2>
```

### Статистика ментора (3 карточки):

```tsx
{/* Первая карточка — без задержки */}
<div className="animate-slide-up">
  Payroll balance
</div>

{/* Вторая — 100ms */}
<div className="animate-slide-up" style={{ animationDelay: "100ms" }}>
  Today
</div>

{/* Третья — 200ms */}
<div className="animate-slide-up" style={{ animationDelay: "200ms" }}>
  Open groups
</div>
```

---

## 🔔 TOAST УВЕДОМЛЕНИЯ

### Keyframes:

```css
/* Появление — сверху с лёгким масштабом */
@keyframes toast-enter {
  from { 
    opacity: 0; 
    transform: translateY(-12px) scale(0.98); 
  }
  to { 
    opacity: 1; 
    transform: translateY(0) scale(1); 
  }
}

/* Исчезновение */
@keyframes toast-exit {
  from { 
    opacity: 1; 
    transform: translateY(0) scale(1); 
  }
  to { 
    opacity: 0; 
    transform: translateY(-8px) scale(0.98); 
  }
}
```

### Применение:

```css
.toast-motion { 
  will-change: transform, opacity; 
}

.toast-motion[data-state="open"] { 
  animation: toast-enter 0.38s var(--easing) both; 
}

.toast-motion[data-state="closed"] { 
  animation: toast-exit 0.32s var(--easing) both; 
}
```

**Визуализация:**
```
ПОЯВЛЕНИЕ (0.38s):
      ↓ Y: -12px, opacity: 0, scale: 0.98
┌─────────────────────────────┐
│  ✅ Успешно                 │  → Y: 0, opacity: 1, scale: 1
│  Действие выполнено         │
└─────────────────────────────┘

ИСЧЕЗНОВЕНИЕ (0.32s):
      ↑ уезжает вверх на 8px
```

---

## 🖱️ HOVER ЭФФЕКТЫ

### Масштабирование карточек:

```css
/* Лёгкое увеличение */
.hover\:scale-101:hover { transform: scale(1.01); }  /* +1% */
.hover\:scale-102:hover { transform: scale(1.02); }  /* +2% */
.hover\:scale-105:hover { transform: scale(1.05); }  /* +5% */
```

### Кнопки:

```css
/* Обычное состояние */
transition-all duration-[var(--dur-fast)]  /* 200ms */

/* Hover */
hover:bg-[#141414]
hover:border-[var(--color-border-hover-1)]  /* #2a2a2a */
hover:text-white

/* Активное нажатие */
active:scale-95  /* Уменьшение на 5% */
```

### Иконки в группах:

```css
/* Статус-иконки в sidebar/карточках */
transition-transform duration-300

/* При hover на родителе */
group-hover:scale-105  /* или scale-110 для активных */
```

---

## ♿ ДОСТУПНОСТЬ

### Отключение анимаций:

```css
@media (prefers-reduced-motion: reduce) {
  .animate-fade-in, 
  .animate-fade-in-up, 
  .animate-slide-in-left,
  .animate-slide-up, 
  .animate-slide-in-bottom, 
  .animate-fade-in-scale,
  .animate-slide-out-left, 
  .animate-fade-out { 
    animation: none !important; 
    opacity: 1 !important; 
  }
}
```

**Что это значит:**
- Пользователи с включённой настройкой "Уменьшить движение" не увидят анимаций
- Все элементы появляются мгновенно
- Нет мерцания и движения

---

## 📊 СВОДНАЯ ТАБЛИЦА АНИМАЦИЙ

| Анимация | Длительность | Easing | Эффект |
|----------|--------------|--------|--------|
| slide-up | 600ms | ease-out | Y +30→0, opacity 0→1 |
| fade-in | 600ms | ease-out | Y +20→0, opacity 0→1 |
| fade-in-up | 500ms | ease-out | Y +30→0, opacity 0→1 |
| slide-in-left | 500ms | ease-out | X -30→0, opacity 0→1 |
| slide-in-bottom | 800ms | ease-out | Y +50→0, opacity 0→1 |
| fade-in-scale | 500ms | ease-out | scale 0.96→1, opacity 0→1 |
| toast-enter | 380ms | --easing | Y -12→0, scale 0.98→1 |
| toast-exit | 320ms | --easing | Y 0→-8, scale 1→0.98 |

---

## 🎯 РЕКОМЕНДАЦИИ ПО ИСПОЛЬЗОВАНИЮ

1. **Каскадные задержки**: увеличивать на 50-100ms для каждого элемента
2. **Заголовки**: появляются раньше контента (200ms)
3. **Карточки**: появляются после заголовка (300ms+)
4. **Footer/мелкие элементы**: появляются последними (1400-1800ms)
5. **Hover-эффекты**: всегда 200-300ms для плавности
6. **Модальные окна**: использовать fade-in-scale

---

*Документация актуальна на 17.01.2025*
